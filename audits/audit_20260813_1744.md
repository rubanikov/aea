# Project Audit — 2026-08-13 17:44

Update to [audit_20260813_1619.md](audit_20260813_1619.md): the app has now been deployed to Railway, closing the previous "no deployed URL" gap. All other findings from the prior audit are unchanged and not repeated in full here — see that file for the complete category-by-category checklist.

## What changed since the last audit

**Deployed to Railway** — project `aea-scheduling-portal`, three services in one project:

| Service | Purpose | URL |
|---|---|---|
| `backend` | Django API (Railpack build, migrations run automatically on deploy) | https://backend-production-e1121.up.railway.app |
| `frontend` | Next.js app (Railpack, zero-config) | https://frontend-production-9ca8.up.railway.app |
| `Postgres` | Managed database, wired to backend via `${{Postgres.DATABASE_URL}}` | internal only |

Verified end-to-end, not just "build succeeded":
- ✅ `GET /health` → `{"status": "ok", "database": "reachable"}`
- ✅ Frontend responds `200` at its root
- ✅ CORS preflight from the frontend origin to the backend succeeds
- ✅ `seed_demo` ran against the live database (15 providers, 25 patients, weekly-recurring cohort included). Net open slots at verification time were ~6,460 on the then-current 56-day image; production was later re-seeded after deploy `83853204` (see addendum: 16,150 gross / 15,014 net / 305 sample bookings).
- ✅ Live login smoke-tested against a seeded demo account (`patient@demo.aea.test`) — returns `200` with the expected user payload, cookies set correctly, custom anti-CSRF header (`X-Requested-With`) enforced as designed
- ✅ `DJANGO_DEBUG=False`, fresh `DJANGO_SECRET_KEY`/`JWT_SECRET_KEY` generated (not the repo's dev placeholders), `SECURE_SSL_REDIRECT`/`*_COOKIE_SECURE` active behind Railway's TLS-terminating proxy
- ⚠️ `RESEND_API_KEY` intentionally left unset on the deployed backend — reminder dispatch will log-and-skip rather than send real emails from a demo deployment (documented, existing graceful-degradation behavior, not a new gap)

README.md and DEMO_CREDENTIALS.md updated in place with the live URLs and a description of what's deployed.

## Checklist deltas from the previous audit

- ❌ → ✅ **Deployed application URL** (Submission Requirements) — now live at the URLs above.
- ⚠️ → ✅ **Encryption at rest / in transit** (Security, Privacy & Compliance) — previously unverifiable because nothing was deployed; now confirmed live (Railway terminates TLS at the edge, Postgres is Railway-managed with encryption at rest by default).
- ⚠️ → ✅ **Uptime ≥99%** (Performance Benchmarks) — previously not measurable with nothing deployed. The app is now live and reachable; note that *sustained* 99% uptime over an eval window still requires an actual monitoring window to pass (a point-in-time health check confirms the app is up now, not a 99% track record — that requires observing it over time).

## Submission requirements — remaining gaps

- ~~**Video demo**~~ — **descoped at candidate's request.** Per instruction, this is no longer tracked as an outstanding requirement in this audit's checklist. Note this is still listed as a required deliverable in `project.md`'s own Submission Requirements and Evaluation Rubric ("Docs, AI disclosure & demo" — 5 pts) — removing it from this audit's tracking doesn't change what the grading rubric asks for, so if the rubric's 5 pts under that row matter for scoring, that gap is still real from a *grader's* perspective even though this audit no longer flags it.
- ⚠️ GitHub repository confirmation — still not independently verified whether the current branch/commits are actually pushed to the `origin` remote (`github.com/rubanikov/aea.git` is configured locally); worth a `git push` check if that matters for submission.

## Bottom line

The one hard blocker from the previous audit — no deployed instance — is now closed, with the deployment independently verified (health check, live login, seeded data), not just assumed from a successful build. The video-demo item has been dropped from this audit's tracking per your instruction; the GitHub-push status is the one remaining loose end worth a quick check before calling submission-ready.

---

## Addendum — 2026-08-13 (ticket 02 closeout)

**Seed horizon (code, ticket 01):** `seed_demo` now uses a **133-day** booking horizon for the 10 k6 providers. Gross computed slots: **16,150** (was 6,800 over 56 days). Sample pre-existing bookings: **305** (was 126). Weekly 5-doctor cohort unchanged — **15 providers / 25 patients** total accounts. `k6/helpers.js` `HORIZON_END_OFFSET_DAYS` updated **54→131** (`QUERY_WINDOW_DAYS=13` unchanged).

**Collision scan default:** `collisions.DEFAULT_HORIZON_DAYS` **90→133** (ticket 01).

**Graceful degradation — closed (no product code in this change):** Researched against `project.md` §7 and prior audit ⚠️. Already met in shipped code:
- `GET /health` returns **503** + `{"status":"degraded","database":"unreachable"}` when Postgres is down (`core/tests/test_health.py`).
- `RESEND_API_KEY` unset: reminder dispatch logs and **skips** without creating a `ReminderLog` row, so the next cron run retries (`reminders/tests/test_services.py::test_a_skipped_send_no_api_key_does_not_create_a_reminder_log_row`). Deployed backend intentionally leaves `RESEND_API_KEY` unset (see ⚠️ line above) — that is spec-compliant skip/retry, not a gap.

**Live Railway re-seed (2026-08-13):**
- `railway run --service backend python manage.py seed_demo` from this workstation **failed**: `DATABASE_URL` points at `postgres.railway.internal`, unreachable outside Railway's network.
- **Succeeded idempotently** via SSH: `railway ssh --service backend /opt/venv/bin/python manage.py seed_demo` — all accounts reported "already exists"; no duplicate rows.
- Production image at first re-seed time was still the **pre-133-day** build (summary: 56-day horizon, 6,800 gross slots, 126 sample bookings). A deploy from the **repo root** (`railway up --service backend` at monorepo root) produced deployment `e2848a59` **FAILED** — Railpack saw `backend/`, `frontend/`, `k6/`, etc. and could not determine how to build (no `start.sh` at root).

**Production deploy + re-seed closeout (2026-08-13 ~19:47 local):**
- **Root cause:** deploy must upload **`backend/` as the archive root**, not the monorepo. Successful prior deploy `69ffab07` used the same pattern (Railpack + `backend/railway.json` NIXPACKS builder, start command from `railway.json`).
- **Fix:** `railway up --service backend --environment production` from **`backend/`** (cwd = service root). New deployment **`83853204-3c04-4efe-9904-0494f93527e6`** → **SUCCESS** (replaces live image `69ffab07`; failed `e2848a59` never served traffic).
- **Re-seed (SSH, post-deploy):** `railway ssh --service backend /opt/venv/bin/python manage.py seed_demo` — idempotent; all 40 demo accounts "already exists". **133-day output confirmed on live DB:**
  - booking horizon: 2026-08-15 .. 2026-12-25 (**133 days**)
  - gross computed slots: **16,150** (was 6,800)
  - pre-existing bookings seeded: **305** (was 126)
  - net open slots: **15,014**
  - weekly cohort unchanged: **15 providers / 25 patients**; weekly window 35 days; recurring bookings already present (0 new)
- **Smoke:** `GET /health` → **200** `{"status":"ok","database":"reachable"}`

**k6 load test (2026-08-13 re-run):** Local Postgres + Waitress, `VUS=30`, `DURATION=60s`, 133-day seed. `slot-availability.js` p95 **252ms**, `booking-action.js` p95 **652ms**, both under `p(95)<1000`, checks 100%. See `k6/README.md`.
