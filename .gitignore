# Env files (never commit secrets — see .env.example)
.env
.env.local
*.env.local

# Local reference only — demo account emails/password (see README.md's
# "Demo credentials" section for the pattern; this file spells out names)
DEMO_CREDENTIALS.md

# OS / editors
.DS_Store
Thumbs.db
.vscode/
.idea/

# Claude Code local tooling state (not part of the project)
.claude/

# Python (backend/) — see backend/.gitignore for the full set
__pycache__/
*.pyc
.venv/

# Node (frontend/) — see frontend/.gitignore, if present, for the full set
node_modules/
.next/
